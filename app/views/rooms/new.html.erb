<h1>新規作成</h1>

  <div class="col-md-6 col-md-offset-3">
    <%= form_for @room do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.submit "Create"%>
    <% end %>
  </div>